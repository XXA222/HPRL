# Hedge contract freeze

`1.0.0` is the shared baseline for all parallel branches. Implementations must
depend on these objects and ports without editing them. A contract change first
requires a written interface-change proposal, compatibility impact assessment,
version increment, and approval before any branch adopts it.
